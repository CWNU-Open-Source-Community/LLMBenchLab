# LLMBenchLab 协作规则

本文件是所有后续 Codex/自动化开发任务的强制规则。它适用于整个仓库；子目录若有更具体规则，应在不削弱本文件的前提下补充。需求优先级依次为：用户明确指令、已接受的 ADR 与协议、本文、一般工程惯例。发生冲突时必须记录并说明。

## 1. 基本原则

- 以“小而完整、可复现、可验证”为准则，一次只推进一个明确阶段或任务。
- 保留仓库已有功能和用户未提交工作；不得覆盖、回滚或格式化无关改动。
- 不静默扩大范围，不为 Roadmap 功能提前引入复杂基础设施。
- 不引入未在计划、依赖清单和工作日志中说明的生产依赖。
- 事实优先：没有实际执行的测试不得写成通过，未完成项不得标记完成。
- 所有时间在持久层使用 UTC；面向用户显示时明确时区。

## 2. 任务开始前的强制步骤

每次任务开始必须按顺序完成：

1. 阅读 `README.md`、本文件、`docs/PROJECT_STATUS.md` 和 `docs/ROADMAP.md`。
2. 根据项目状态阅读当前阶段文档（`docs/phases/PHASE-*.md`）及与改动相关的协议、API、安全和测试文档。
3. 运行 `git status --short --branch`，检查仓库结构和已有改动；不得假定工作区干净。
4. 识别本任务会接触的文件以及必须保留的用户改动。发现重叠且无法安全合并时，停止并说明阻碍。
5. 在 `docs/worklogs/` 新建本次工作日志，命名为 `YYYY-MM-DD-<task-slug>.md`；同日同名时增加清晰后缀，不能覆盖旧日志。
6. 在日志中先写明目标、背景、范围、非目标、验收标准、假设、风险和实施步骤。
7. 对跨模块、数据库、公共 API、Benchmark 协议或安全边界的复杂任务，按 `PLANS.md` 建立并维护执行计划。
8. 若架构、协议或关键取舍发生变化，实施前新增 ADR；若是修正既有决定，则新增替代 ADR 或更新尚未接受的 ADR，不得悄悄改变约定。

初始勘察只做只读检查。禁止 `git reset --hard`、强制推送、未经授权修改 remote/上传仓库，或删除范围不明的数据。本仓库已由用户明确授权使用 `https://github.com/CWNU-Open-Source-Community/LLMBenchLab.git` 作为 `origin`；阶段收尾的普通 push 按第 5 节执行，仍不得强制推送。

## 3. 实施规则

### 3.1 范围与代码质量

- 计划中的每一步都应产生可检查的结果；范围变化要先更新计划和日志。
- 核心逻辑不得以 `TODO`、`pass`、伪实现或仅返回固定成功值代替。
- 不吞掉异常。可恢复的单题错误必须保存类型和可读原因；不可恢复错误必须使 Run 进入正确终态。
- 使用清晰类型；公开接口和非显然算法应有必要注释或 docstring。
- 数据库模型与 API Schema、Adapter 与 Evaluator、模型原始输出与解析答案、执行与计分逻辑必须分离。
- 避免巨型单文件、循环依赖和没有当前用途的抽象。
- 新行为必须有测试；修复缺陷时优先添加能复现缺陷的测试。

### 3.2 变更联动

- 行为或用户操作方式变化：同步更新相关文档和 README。
- 数据库结构变化：提供可前进执行的 Alembic migration，说明兼容和回滚策略。
- API 路径、Schema、状态码或错误格式变化：更新 `docs/API.md` 及相关测试。
- Benchmark 格式、Hash、Prompt、Evaluator 或评分语义变化：更新 `docs/BENCHMARK_PROTOCOL.md`、`docs/DATASET_FORMAT.md`，并按规则提升 `protocol_version` 或数据 schema 版本。
- 安全边界变化：更新 `docs/SECURITY.md`，必要时新增 ADR。
- 新增依赖：说明目的，固定兼容版本或更新 lockfile，并确认许可证和维护风险。

### 3.3 评测与数据纪律

- 自动化测试、CI 和 Smoke Test 只能使用 `MockModelAdapter`，不得调用真实或付费模型。
- 默认严格总分以全部计划题目为分母；请求错误、空回答和解析失败计 0，同时独立报告 `completion_rate` 与 `answered_accuracy`。
- 不同 `protocol_version`、Benchmark version 或 dataset hash 的结果不得无提示混合比较。
- 每次 Run 必须保存协议要求的配置与数据快照；无法获得 Git SHA 时明确保存 `null`。
- Demo 数据必须显著标记为演示数据，不能冒充正式模型能力结论。

### 3.4 安全底线

- 不写入、提交、回显或记录真实 API Key、Authorization header、Cookie 或其他凭据。
- 数据库仅保存 `api_key_env`（环境变量名称），运行时再读取对应值；响应 Schema 不得暴露密钥值。
- `.env` 不提交，`.env.example` 只能放安全占位符。
- 不使用 `eval`，不执行 Benchmark 中的代码，不允许 manifest 引用任意本地路径。
- 数据集导入必须限制大小、阻止路径穿越并返回明确的行号/字段错误。
- 自定义 `base_url` 带来 SSRF 风险；MVP 不得宣称适合直接暴露公网。
- 日志和上游错误返回前必须脱敏。若意外发现密钥，停止传播并记录处置建议。

## 4. 验证要求

按改动风险选择最小但充分的验证集合，并优先使用仓库统一命令：

- 后端：目标单测、完整 `pytest`、Ruff lint/format check。
- 前端：目标组件测试、Vitest、ESLint、TypeScript typecheck、production build。
- 端到端链路：完全离线的 Mock smoke test。
- 部署配置：`docker compose config`；若本机 Docker 不可用，只能记录未运行及原因。
- 文档：检查内部链接、命令、版本号、状态和实际实现一致。

测试失败时不得降低断言、跳过关键用例或伪造结果。环境缺依赖或禁止联网时，应继续完成可离线工作，记录失败命令、原始原因和用户可执行的后续命令。

## 5. 任务结束前的强制步骤

1. 运行与改动相关的测试、lint 和必要构建；记录命令、通过/失败、测试数和失败原因。
2. 检查 diff，确认没有秘密信息、调试残留、无关改动或虚假的完成标记。
3. 更新 `CHANGELOG.md`。
4. 更新 `docs/PROJECT_STATUS.md`，只陈述已验证事实。
5. 更新当前阶段文档的状态、验收证据和遗留项。
6. 更新 `docs/NEXT_TASK.md`，提供可直接执行的下一任务说明。
7. 完成本次工作日志中的实际修改、实际命令、结果、已知问题、未完成项和下一步。
8. 每个阶段形成独立、可审查的 commit；提交前复核 staged diff 和秘密扫描，不把失败或未验证状态写成完成。
9. 将阶段 commit push 到 `origin`。常规开发应 push 工作分支并通过 PR 触发 CI；仅在用户明确授权直接交付当前分支时 push `main`。禁止 force push。
10. 等待**该精确 commit SHA** 的 GitHub Actions 必需 job 全部成功。CI 失败时读取日志、修复、重新 commit/push 并再次等待；在绿色之前阶段保持 `in_progress`。认证、权限或 GitHub 服务阻塞时必须如实记录，不能把本地通过替代为远程 CI 通过。
11. 最终回复给出修改文件摘要、实际运行命令与结果、远程 commit/branch、CI 链接与结论、未运行验证、已知限制、安全检查和 `git status --short` 摘要。
12. Issue、PR 合并、Release、tag、仓库设置或其他额外远程变更仍须在用户授权范围内执行；阶段 push/CI 的长期授权不等于这些操作的通用授权。

## 6. Definition of Done

任务只有同时满足以下条件才可称为完成：

- 约定范围内的功能与验收标准已实现，无关键路径占位实现。
- 相关测试、lint、类型检查和构建已实际运行并通过；任何未运行项都已明确披露且不影响错误地宣称完成。
- 数据迁移、API、协议、数据格式、安全和部署文档已按变更类型同步。
- 代码不包含真实密钥，不会在日志或 API 中泄漏秘密，自动测试未访问真实模型。
- 错误路径、空状态和边界输入得到处理；Demo 与正式结果明确区分。
- `CHANGELOG.md`、`docs/PROJECT_STATUS.md`、当前阶段文档、`docs/NEXT_TASK.md` 和工作日志与仓库真实状态一致。
- 变更范围干净，不覆盖用户工作，不包含无关生成物；已知限制和后续工作可追踪。
- 每个阶段已有独立 commit 并 push 到 `origin`；该精确 SHA 的 GitHub Actions 必需 job 全部成功。仅本地测试通过不能替代远程门禁。
- 若任务属于 Phase 验收，阶段文档列出的每项验收标准都有可复核证据；否则状态保持 `in_progress`。
